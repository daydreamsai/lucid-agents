# Payment support matrix

<!-- Generated by scripts/generate-payment-support-matrix.ts from docs/payment-support-matrix.json. Do not edit directly. -->

This matrix describes Lucid's shipped payment surface. “Built in” means Lucid
registers and coordinates the implementation; it does not remove the need for
credentials, accounts, network access, or provider configuration.

- x402 SDK cohort: exact `2.19.0`
- MPP SDK: exact `mppx 0.8.14`
- Durable adapters coordinate replicas and survive restarts. Process-local
  defaults are intended for development and single-process deployments.

| Protocol / SDK | Scheme / method | Network | Buyer | Seller | Facilitator dependency | Transport | Storage / durability | Status / limitations |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| x402 v2<br>@x402/* 2.19.0 | exact | EVM (`eip155:*`) | Built in | Built in | Required for verification and settlement | invoke: yes; stream/SSE: yes; task: yes | Payment-policy accounting is process-local by default; SQLite and Postgres are durable options. | **Released.** A facilitator must advertise the exact scheme on the configured network. |
| x402 v2<br>@x402/* 2.19.0 | exact | Solana (`solana:*`) | External x402 client | Built in | Required for verification and settlement | invoke: yes; stream/SSE: yes; task: yes | Payment-policy accounting is process-local by default; SQLite and Postgres are durable options. | **Released.** Lucid's built-in paid fetch registers EVM schemes only. |
| x402 v2<br>@x402/* 2.19.0 | upto | EVM (`eip155:*`) | Built in | Built in | Required; `/supported` must include `facilitatorAddress` | invoke: yes; stream/SSE: no; task: no | The accepted ceiling is reserved; memory, SQLite, and Postgres atomically replace it with the handler's actual amount. | **Released.** Invoke only. The handler must return a non-negative `payment.actualAmount` no greater than the accepted ceiling. |
| x402 v2<br>@x402/* 2.19.0 | batch-settlement | EVM (`eip155:*`) | Built in when enabled | Built in when enabled | Required for channel verification and settlement | invoke: yes; stream/SSE: yes; task: yes | Development may use bounded memory. Production sellers require injected durable storage; SQLite and Postgres subpaths are provided. Buyers may inject upstream channel storage for restart continuation. | **Released.** EVM only. Seller configuration is explicit, and every route resolves to one static receiver. |
| MPP<br>mppx 0.8.14 | tempo / charge | Tempo mainnet or testnet | Via `mppx/client` intent | Built in | Not required | invoke: yes; stream/SSE: yes; task: yes | Challenge replay fences are bounded memory by default, with durable SQLite and Postgres adapters. | **Released.** SSE is protected by one charge before streaming; it is not a metered Tempo session. |
| MPP<br>mppx 0.8.14 | tempo / session | Tempo mainnet or testnet | Via `mppx/client` session intent | Built in with explicit `tempo.session()` | Not required | invoke: yes; stream/SSE: yes; task: no | Development may use bounded process memory. Production requires an atomic durable store; SQLite and Postgres are exported from the MPP storage subpaths. | **Released.** Tempo TIP-1034 only. Invoke deducts one configured unit; SSE reserves a bounded deposit ceiling and reconciles delivered units. Scheduled settlement requires a signing server account. |
| MPP<br>mppx 0.8.14 | stripe / charge | Stripe Business Network | Via `mppx/client` intent | Built in | Not required | invoke: yes; stream/SSE: yes; task: yes | Challenge replay fences are bounded memory by default, with durable SQLite and Postgres adapters. | **Released.** Requires a Stripe secret and Business Network profile. Session intent is not built in. |
| MPP<br>mppx 0.8.14 | evm / charge (x402 exact compatible) | Configured EVM chain ID | MPP EVM or compatible x402 exact client | Built in | Optional: choose a facilitator or application settlement callback | invoke: yes; stream/SSE: yes; task: yes | Challenge replay fences are bounded memory by default, with durable SQLite and Postgres adapters. | **Released.** Charge only. Requires EIP-3009 token-domain metadata and exactly one settlement strategy; SSE settles once before streaming. |
| MPP<br>mppx 0.8.14 | evm / session | Configured EVM chain ID | No Lucid-native session client | Not built in | Application-defined | invoke: custom; stream/SSE: custom; task: custom | A custom method owns session state and durability. | **Custom only.** The native EVM descriptor materializes `evm.charge()` only. |
| MPP<br>mppx 0.8.14 | custom / charge or session | Application-defined | Application or compatible `mppx` method | Custom verifier | Application-defined | invoke: yes; stream/SSE: yes; task: yes | Lucid can durably fence challenges with SQLite/Postgres; the verifier owns external settlement and session deduplication. | **Extension point.** The verifier must validate and settle the complete method contract, return a legal receipt, and deduplicate external side effects. |
| MPP<br>mppx 0.8.14 | lightning / custom | Lightning | Application-defined | Descriptor plus custom verifier | Application-defined | invoke: yes; stream/SSE: yes; task: yes | Challenge replay fences are bounded memory by default, with durable SQLite and Postgres adapters. | **Custom only.** Lucid supplies configuration metadata but no built-in Lightning verification or settlement. |

Regenerate this document with `bun run docs:payment-support`. CI verifies the
generated file, SDK pins, package export maps, and packed storage imports with
`bun run check:payment-support`.
