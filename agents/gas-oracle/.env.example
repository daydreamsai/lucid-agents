# Gas Oracle — Environment Variables

# Port to bind to (default: 3000)
PORT=3000

# Agent base URL (used for self-reference in manifests)
AGENT_ORIGIN=http://localhost:3000

# ─── RPC Endpoints ───────────────────────────────────────────────────────────
# Fallback RPC URL used when chain-specific var is absent
RPC_URL=https://eth.llamarpc.com

# Per-chain overrides (replace CHAIN with the chain name in UPPER_SNAKE_CASE)
RPC_URL_ETHEREUM=https://eth.llamarpc.com
RPC_URL_POLYGON=https://polygon.llamarpc.com
RPC_URL_ARBITRUM=https://arbitrum.llamarpc.com
RPC_URL_OPTIMISM=https://optimism.llamarpc.com
RPC_URL_BASE=https://base.llamarpc.com

# ─── Payments (x402 / @lucid-agents/payments) ────────────────────────────────
# Wallet address that receives micropayment fees
PAYMENTS_RECEIVABLE_ADDRESS=0xYourWalletAddressHere

# Network for payments (ethereum|base|polygon|arbitrum|optimism)
NETWORK=base

# x402 facilitator URL
FACILITATOR_URL=https://facilitator.daydreams.systems

# ─── Lucid Agent Identity (optional) ─────────────────────────────────────────
AGENT_DOMAIN=gas-oracle.yourdomain.com
PRIVATE_KEY=0xYourPrivateKeyHere
RPC_URL_IDENTITY=https://eth.llamarpc.com
IDENTITY_REGISTRY_ADDRESS=

# ─── Integration Tests ───────────────────────────────────────────────────────
# Set to "true" to run live RPC integration tests
INTEGRATION_TESTS=false
